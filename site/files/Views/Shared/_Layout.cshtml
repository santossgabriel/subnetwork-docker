<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fake Bank</title>
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header class="toolbar">
        <div>
            <img class="logo" src="~/favicon.ico" />
            <span class="app-title">Fake Bank</span>
        </div>
        <div class="menu-toolbar">
            <a asp-area="" asp-controller="Home" asp-action="Privacy">Home</a>
            <a asp-area="" asp-controller="Simulation" asp-action="Index">Simulador</a>
            <a asp-area="" asp-controller="Contact" asp-action="">Contato</a>
        </div>
        @if (User.Identity.IsAuthenticated)
        {
            <span class="logged-name">@User.Identity.Name</span>
            <a class="btn" asp-area="" asp-controller="Home" asp-action="Logout">Sair</a>
        }
        else
        {
            <form asp-controller="Home" asp-action="Login" class="login-form">
                <input class="form-input" name="email" placeholder="Email">
                <input class="form-input" type="password" name="password" placeholder="Senha">
                <button class="btn">Entrar</button>
            </form>
        }
    </header>
    <div class="root-card">
        @RenderBody()
    </div>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>